# Base de données PostgreSQL
DATABASE_URL="postgresql://ludimmo:ludimmo_dev@localhost:5432/chantiers_essaouira?schema=public"

# Serveur
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# JWT (générer avec: openssl rand -base64 64)
JWT_SECRET="REMPLACE_PAR_UNE_VRAIE_CLE_64_CHARS_EN_PROD"
JWT_REFRESH_SECRET="REMPLACE_PAR_UNE_AUTRE_CLE_DIFFERENTE_64_CHARS"

# CORS — origines autorisées (séparées par virgule)
CORS_ORIGINS="http://localhost:5173,http://localhost:4173"

# Stockage uploads (en local, /var/app/uploads en prod)
UPLOAD_DIR="./uploads"
